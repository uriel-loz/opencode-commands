{
  "type": "module",
  "name": "opencode-commands",
  "version": "1.0.0",
  "description": "Command prompts para opencode agents",
  "main": "dist/configure.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "start": "node scripts/install.mjs"
  },
  "dependencies": {
    "@clack/prompts": "^1.5.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^5.0.0"
  }
}
